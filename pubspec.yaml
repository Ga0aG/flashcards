name: flashcards
description: A flashcard language learning app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  path: ^1.8.3
  audioplayers: ^5.2.0
  flutter_tts: ^4.0.2
  http: ^1.1.0
  provider: ^6.1.0
  uuid: ^4.0.0
  shared_preferences: ^2.2.0
  localstorage: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
